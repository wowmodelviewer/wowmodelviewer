#include "CharInfos.h"

CharInfos::CharInfos() : valid(false), customTabard(false), raceId(0), gender(""),
						 hasTransmogGear(false), eyeGlowType(0), tabardIcon(-1),
						 iconColor(-1), tabardBorder(-1), borderColor(-1), background(-1)
{
}
