name: Build

on:
  push:
    branches: [ develop ]
    paths-ignore:
      - '**.md'
      - 'doc/**'
      - '.gitignore'
      - '.gitattributes'
  workflow_dispatch:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      # Primary cache:
      # Cache is invalidated automatically when cmake/dependencies.cmake changes.
      # restore-keys lets a partial match seed the cache so only changed
      # packages need to be rebuilt by vcpkg.
      # Only cache generated artifacts — exclude git submodules (glm, casclib).
      - name: Cache ThirdParty
        uses: actions/cache@v5
        with:
          path: |
            ThirdParty/lib/x64
            ThirdParty/include/fbxsdk.h
            ThirdParty/include/fbxsdk
            bin_support/vcredist_x64.exe
          key: ThirdParty-${{ runner.os }}-${{ hashFiles('cmake/dependencies.cmake') }}
          restore-keys: |
            ThirdParty-${{ runner.os }}-

      - name: Set up MSVC x64 environment
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vsPath = & $vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          $vcvarsall = Join-Path $vsPath "VC\Auxiliary\Build\vcvarsall.bat"
          cmd /c "`"$vcvarsall`" x64 && set" | ForEach-Object {
            if ($_ -match '^([^=]+)=(.*)$') {
              "$($Matches[1])=$($Matches[2])" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            }
          }

      - name: Set up Python for glad2
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install glad2 Python dependencies
        run: pip install jinja2

      - name: Configure
        shell: pwsh
        run: |
          $env:VCPKG_ROOT = $env:VCPKG_INSTALLATION_ROOT
          cmake --preset x64-Release -DBUILDNUMBER=${{ github.run_number }} -DVCPKG_OVERLAY_TRIPLETS="${{ github.workspace }}/scripts/vcpkg-triplets"

      - name: Build
        run: cmake --build --preset x64-Release

      - name: Rename installer
        shell: pwsh
        run: Rename-Item "bin\WMV_Installer.exe" "WMV_Installer_v0.11.0.${{ github.run_number }}.exe"

      - name: Upload installer artifact
        uses: actions/upload-artifact@v7
        with:
          name: WMV_Installer_v0.11.0.${{ github.run_number }}
          path: bin\WMV_Installer_v*.exe

      - name: Notify Discord
        if: always()
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.WEBHOOK_URL }}
          status: ${{ job.status }}
