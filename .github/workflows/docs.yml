# ---------------------------------------------------------------------------
# .github/workflows/docs.yml — Build & deploy Doxygen docs to GitHub Pages
# ---------------------------------------------------------------------------
# Triggers on pushes to 'develop' that touch Source/ or docs/.
#
# Prerequisites (one-time repo setup):
#   Settings → Pages → Source → "GitHub Actions"
# ---------------------------------------------------------------------------

name: Documentation

on:
  push:
    branches: [develop]
    paths:
      - 'Source/**'
      - 'docs/**'
      - 'readme.md'
      - 'cmake/docs.cmake'
      - '.github/workflows/docs.yml'
  workflow_dispatch:   # allow manual trigger

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true          # needed for doxygen-awesome-css

      - name: Install Doxygen & Graphviz
        run: sudo apt-get update && sudo apt-get install -y doxygen graphviz

      - name: Generate Doxyfile from template
        run: |
          sed -e "s|@CMAKE_SOURCE_DIR@|$GITHUB_WORKSPACE|g" \
              -e "s|@DOXYGEN_OUTPUT_DIR@|$GITHUB_WORKSPACE/build/docs|g" \
              -e "s|@DOXYGEN_AWESOME_DIR@|$GITHUB_WORKSPACE/ThirdParty/doxygen-awesome-css|g" \
              -e "s|@DOXYGEN_HAVE_DOT@|YES|g" \
              docs/Doxyfile.in > Doxyfile

      - name: Generate documentation
        run: doxygen Doxyfile

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/docs/html

  deploy:
    needs: build-docs
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
